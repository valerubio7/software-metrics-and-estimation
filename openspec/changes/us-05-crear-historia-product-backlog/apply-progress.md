# Progreso de aplicación: US-05

## Corte 1 — planificación base

- Base: `main` en `b99eb58`; rama local `docs/us05-plan-base`; entrega encadenada hacia `main`, sin push ni PR.
- Tarea 1: revisión de exploración, propuesta, especificación y diseño; casilla de `tasks.md` a marcar solo tras observar el commit y sus comprobaciones. Tareas 2–10 pendientes.
- Archivos del corte: `exploration.md`, `proposal.md`, `specs/historia/spec.md`, `design.md` y este progreso mínimo. `tasks.md` queda fuera del commit (corte 2).
- Coherencia: los cuatro documentos distinguen decisiones históricas de exploración/propuesta de contrato posterior; proyecto conocido, prioridad exacta, criterios no vacíos, estado `pendiente`, puntos `null`, FK sin historias huérfanas. Las notas anteriores sobre estrategia pendiente no invalidan la decisión posterior registrada en `tasks.md`.
- Referencias comprobadas: `internal/api/api.go` registra solo `POST /projects`; existen la migración `000001` y las rutas de código y pruebas de US-01 citadas.
- Comprobaciones previas al commit: estructura de documentos y rutas revisadas; `git diff --cached --check` sin errores; `git diff --cached --numstat`: cinco archivos, +241/−0 = 241 líneas (incluido este progreso), dentro del máximo de 400. No existe `POST /projects/{project_id}/stories` en `internal/api/api.go`.
- TDD estricto: RED/GREEN/TRIANGULATE/REFACTOR y `go test ./...` no aplican a este corte exclusivamente documental; no se ejecutaron pruebas de aplicación ni se inventa evidencia runtime.
- Desviaciones de diseño: ninguna. Reversión: retirar únicamente estos documentos y este progreso, sin tocar código, datos ni US-01.
- Estado nativo consumido: `gentle-ai.sdd-status` v2, cambio US-05, `applyState: ready`, `nextRecommended: apply`, sin bloqueos; `actionContext: repo-local` con raíz y ediciones autorizadas dentro de este repositorio, sin advertencias.
- Límite: detenerse después del corte 1; no ejecutar tarea 2 ni modificar fuentes.

## Resultado observado después del commit (registro local no incluido en el corte 1)

- Commit local `654dcb8` (`docs(us05): record base story planning decisions`), base `b99eb58`; cinco rutas comprometidas: los cuatro documentos base y este progreso. `tasks.md` no entró al commit.
- Diff `main...HEAD`: +241/−0 = 241 líneas; `git diff main...HEAD --check` sin errores. Casilla 1 marcada `[x]` en `tasks.md` después de comprobar el commit; el archivo permanece sin seguimiento para el corte 2.
- No hubo pruebas de aplicación ni ejecución de RED/GREEN/TRIANGULATE/REFACTOR porque no se cambió comportamiento runtime. No se hizo push, PR, merge ni borrado. Estado nativo de salida no consultado: la proyección de entrada fue `apply` listo y este corte termina con tareas pendientes.
- Al cierre del corte 1 quedaban pendientes las tareas 2–10; la tarea 2 se aborda exclusivamente en el corte documental siguiente.

## Corte 2 — plan operativo y evidencia

- Dependencia real: `docs/us05-tasks` nació de `docs/us05-plan-base` en `654dcb8`; PR 1 todavía no se integró en `main`. Estrategia `stacked-to-main`: PR 2 solo apuntará a `main` una vez integrada PR 1 y verificado el diff aislado. 📍 Corte 2 de 6; sigue PR 3 (núcleo interno). Sin push, PR, merge ni eliminación de ramas.
- Alcance: `tasks.md` (plan, casilla 1 previa y casilla 2 tras validación) y esta evidencia incremental; no se tocaron propuesta, especificación, diseño, código, pruebas ni migraciones. Reversión: revertir únicamente el commit de este corte; PR 1 permanece intacta.
- Coherencia documental: cuatro campos de entrada, prioridad exacta, criterios no vacíos, estado `pendiente`, puntos nulos, FK antes de habilitar HTTP y migración externa antes de exposición; las tareas 3–10 preservan RED → GREEN → TRIANGULATE → REFACTOR.
- Comprobación antes de marcar tarea 2: `git diff --cached --check` sin errores, `git diff --cached --numstat` contra `654dcb8` +88/−0 (25 de progreso y 63 de tareas); comprobación automatizada de diez casillas (1 marcada, 2–10 pendientes), guardas, dependencia local, ruta/migración y coincidencia de tareas 3–10 entre ambos archivos: OK. Tras estas observaciones, se marca tarea 2; la medición definitiva del commit incluye la casilla y esta evidencia.
- TDD estricto: documentación pasiva; `go test ./...` no se ejecutó. Runtime harness: N/A, ninguna conducta de aplicación cambia. Desviaciones del diseño: ninguna.

| TDD Cycle Evidence | RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- |
| Tarea 2, solo documentación | N/A, sin prueba runtime | N/A, sin código | N/A, sin conducta | N/A, sin código |

- Estado nativo consumido: `gentle-ai.sdd-status` v2, `applyState: ready`, `nextRecommended: apply`, 1/10 tareas completas, `blockedReasons: []`, `notes: []`; `actionContext: repo-local`, raíz del repositorio y único root permitido en este repositorio, sin advertencias. Tras este corte quedan tareas 3–10; no iniciar implementación de núcleo.
- Trabajo restante (líneas exactas sin marcar en `tasks.md`):

- [ ] 3. **RED:** en `tests/unit/story/domain/story_test.go` y `tests/unit/story/application/create_story_test.go`, probar título/descripción en blanco (ASCII/Unicode), prioridad exacta `alta/media/baja`, criterios ausentes/nulos/vacíos o con entrada en blanco, conservación de texto/orden, estado `pendiente`, puntos ausentes, ID generado, una escritura válida, cero ante invalidación y propagación de error tipado de proyecto ausente o inesperado; ejecutar `go test ./...` y registrar el RED.
- [ ] 4. **GREEN → TRIANGULATE → REFACTOR:** implementar `internal/story/domain/story.go` y `internal/story/application/create_story.go` con validación por campo, comando limitado a ProjectID y cuatro entradas, generador ID, puerto `Create(ctx, story) error` y `ErrProjectNotFound` desacoplado de pgx. Reejecutar `go test ./...` tras GREEN; agregar primero un borde RED (p. ej. criterios Unicode), resolverlo y simplificar sin ampliar contrato, dejando el runner verde tras cada etapa final. No introducir lectura previa ni aceptar ID/estado/puntos del cliente.
- [ ] 5. **RED:** crear `tests/integration/story/postgres/repository_integration_test.go` con Testcontainers y migraciones `000001` + `000002` (referencia: `tests/integration/project/postgres/repository_integration_test.go`): proyecto presente/faltante, fila y orden `TEXT[]`, `story_points IS NULL`, `23503` de FK nombrada frente a otro `23503`/error inesperado, rechazo de borrado de proyecto referenciado y órdenes controlados inserción/borrado sin huérfanos. Ejecutar `go test ./...` y registrar RED; Docker requerido, skip no acredita RED de persistencia.
- [ ] 6. **GREEN → TRIANGULATE → REFACTOR:** añadir `internal/project/infrastructure/postgres/migrations/000002_create_stories.{up,down}.sql` y `internal/story/infrastructure/postgres/repository.go`: PK/FK nombrada `stories_project_id_fkey`, checks básicos, `story_points INTEGER NULL` (US-14 podrá actualizarlo), INSERT único parametrizado sin SELECT, traducir solo `23503` de esta FK a `ErrProjectNotFound`; no reintentar resultado de red incierto. Ejecutar `go test ./...` tras GREEN, añadir borde RED de integridad y resolverlo, simplificar, y al cerrar ejecutar runner y comando de integración con Docker **sin skips**. Si la carrera o Testcontainers elevan el diff hacia 400, separar un borde de integridad en otro PR interno completo antes del endpoint, no omitir pruebas.
- [ ] 7. **RED:** crear `tests/unit/story/transport/http/handler_test.go` con `PathValue` en requests directas y casos `201` (ID/ProjectID UUID, `pendiente`, prioridad, criterios ordenados, clave `story_points:null`), `400 invalid_request` fijo para JSON mal formado, múltiples valores, tipos, cuerpo `null`/array y claves extra incluidas `id`, `project_id`, `status`, `story_points` aun con null/0; `422 validation_failed` para UUID y entradas inválidas sin escritura, `404` solo error tipado y `500` genérico sin secretos; ejecutar `go test ./...` y registrar RED.
- [ ] 8. **GREEN → TRIANGULATE → REFACTOR:** implementar `internal/story/transport/http/handler.go` con objeto JSON único, `DisallowUnknownFields`, UUID normalizado antes de DB, mapeos seguros 400/422/404/500 y `story_points` nulo sin `omitempty`; ejecutar `go test ./...` tras GREEN, añadir primero un caso RED de rechazo (p. ej. Unicode/prioridad con mayúsculas), resolverlo y refactorizar con runner verde. No registrar la ruta en `internal/api/api.go` en este corte.
- [ ] 9. **RED:** ampliar `tests/unit/cmd/api/main_test.go` para registro de nueva ruta/método no creador y preservación `POST /projects`; agregar prueba HTTP real a `tests/integration/story/postgres/repository_integration_test.go` (o archivo concreto `tests/integration/story/postgres/http_integration_test.go`) con migraciones aplicadas, alta y proyecto inexistente. Ejecutar `go test ./...` y registrar RED con Docker; no acreditar integración omitida.
- [ ] 10. **GREEN → TRIANGULATE → REFACTOR:** adaptar `internal/api/api.go` y `cmd/api/main.go` para inyectar repo PostgreSQL/generador UUID, registrar ruta solo tras disponibilidad de 000002; actualizar `README.md` con request/response, cuatro campos, ProjectID conocido, comando `migrate ... up` previo al servidor y advertencia sobre rollback con datos. Ejecutar `go test ./...` tras GREEN; añadir un caso RED de composición/HTTP, resolverlo y refactorizar; ejecutar runner e integración Docker reales al cierre, comprobar diff ≤400 incluyendo README/tests y verificar manualmente alta/rechazo contra DB migrada. No modificar US-01 ni cerrar #33 antes de la verificación final.
