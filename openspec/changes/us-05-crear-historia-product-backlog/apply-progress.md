# Progreso de aplicación: US-05

## Corte 1 — planificación base

- Base: `main` en `b99eb58`; rama local `docs/us05-plan-base`; entrega encadenada hacia `main`, sin push ni PR.
- Tarea 1: revisión de exploración, propuesta, especificación y diseño; casilla de `tasks.md` a marcar solo tras observar el commit y sus comprobaciones. Tareas 2–10 pendientes.
- Archivos del corte: `exploration.md`, `proposal.md`, `specs/historia/spec.md`, `design.md` y este progreso mínimo. `tasks.md` queda fuera del commit (corte 2).
- Coherencia: los cuatro documentos distinguen decisiones históricas de exploración/propuesta de contrato posterior; proyecto conocido, prioridad exacta, criterios no vacíos, estado `pendiente`, puntos `null`, FK sin historias huérfanas. Las notas anteriores sobre estrategia pendiente no invalidan la decisión posterior registrada en `tasks.md`.
- Referencias comprobadas: `internal/api/api.go` registra solo `POST /projects`; existen la migración `000001` y las rutas de código y pruebas de US-01 citadas.
- Comprobaciones previas al commit: estructura de documentos y rutas revisadas; `git diff --cached --check` sin errores; `git diff --cached --numstat`: cinco archivos, +241/−0 = 241 líneas (incluido este progreso), dentro del máximo de 400. No existe `POST /projects/{project_id}/stories` en `internal/api/api.go`.
- TDD estricto: RED/GREEN/TRIANGULATE/REFACTOR y `go test ./...` no aplican a este corte exclusivamente documental; no se ejecutaron pruebas de aplicación ni se inventa evidencia runtime.
- Desviaciones de diseño: ninguna. Reversión: retirar únicamente estos documentos y este progreso, sin tocar código, datos ni US-01.
- Estado nativo consumido: `gentle-ai.sdd-status` v2, cambio US-05, `applyState: ready`, `nextRecommended: apply`, sin bloqueos; `actionContext: repo-local` con raíz y ediciones autorizadas dentro de este repositorio, sin advertencias.
- Límite: detenerse después del corte 1; no ejecutar tarea 2 ni modificar fuentes.

## Resultado observado después del commit (registro local no incluido en el corte 1)

- Commit local `654dcb8` (`docs(us05): record base story planning decisions`), base `b99eb58`; cinco rutas comprometidas: los cuatro documentos base y este progreso. `tasks.md` no entró al commit.
- Diff `main...HEAD`: +241/−0 = 241 líneas; `git diff main...HEAD --check` sin errores. Casilla 1 marcada `[x]` en `tasks.md` después de comprobar el commit; el archivo permanece sin seguimiento para el corte 2.
- No hubo pruebas de aplicación ni ejecución de RED/GREEN/TRIANGULATE/REFACTOR porque no se cambió comportamiento runtime. No se hizo push, PR, merge ni borrado. Estado nativo de salida no consultado: la proyección de entrada fue `apply` listo y este corte termina con tareas pendientes.
- Al cierre del corte 1 quedaban pendientes las tareas 2–10; la tarea 2 se aborda exclusivamente en el corte documental siguiente.

## Corte 2 — plan operativo y evidencia

- Dependencia real: `docs/us05-tasks` nació de `docs/us05-plan-base` en `654dcb8`; PR 1 todavía no se integró en `main`. Estrategia `stacked-to-main`: PR 2 solo apuntará a `main` una vez integrada PR 1 y verificado el diff aislado. 📍 Corte 2 de 6; sigue PR 3 (núcleo interno). Sin push, PR, merge ni eliminación de ramas.
- Alcance: `tasks.md` (plan, casilla 1 previa y casilla 2 tras validación) y esta evidencia incremental; no se tocaron propuesta, especificación, diseño, código, pruebas ni migraciones. Reversión: revertir únicamente el commit de este corte; PR 1 permanece intacta.
- Coherencia documental: cuatro campos de entrada, prioridad exacta, criterios no vacíos, estado `pendiente`, puntos nulos, FK antes de habilitar HTTP y migración externa antes de exposición; las tareas 3–10 preservan RED → GREEN → TRIANGULATE → REFACTOR.
- Comprobación antes de marcar tarea 2: `git diff --cached --check` sin errores, `git diff --cached --numstat` contra `654dcb8` +88/−0 (25 de progreso y 63 de tareas); comprobación automatizada de diez casillas (1 marcada, 2–10 pendientes), guardas, dependencia local, ruta/migración y coincidencia de tareas 3–10 entre ambos archivos: OK. Tras estas observaciones, se marca tarea 2; la medición definitiva del commit incluye la casilla y esta evidencia.
- TDD estricto: documentación pasiva; `go test ./...` no se ejecutó. Runtime harness: N/A, ninguna conducta de aplicación cambia. Desviaciones del diseño: ninguna.

| TDD Cycle Evidence | RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- |
| Tarea 2, solo documentación | N/A, sin prueba runtime | N/A, sin código | N/A, sin conducta | N/A, sin código |

- Estado nativo consumido: `gentle-ai.sdd-status` v2, `applyState: ready`, `nextRecommended: apply`, 1/10 tareas completas, `blockedReasons: []`, `notes: []`; `actionContext: repo-local`, raíz del repositorio y único root permitido en este repositorio, sin advertencias. Tras este corte quedan tareas 3–10; no iniciar implementación de núcleo.
- Trabajo restante (líneas exactas sin marcar en `tasks.md`):

- [ ] 3. **RED:** en `tests/unit/story/domain/story_test.go` y `tests/unit/story/application/create_story_test.go`, probar título/descripción en blanco (ASCII/Unicode), prioridad exacta `alta/media/baja`, criterios ausentes/nulos/vacíos o con entrada en blanco, conservación de texto/orden, estado `pendiente`, puntos ausentes, ID generado, una escritura válida, cero ante invalidación y propagación de error tipado de proyecto ausente o inesperado; ejecutar `go test ./...` y registrar el RED.
- [ ] 4. **GREEN → TRIANGULATE → REFACTOR:** implementar `internal/story/domain/story.go` y `internal/story/application/create_story.go` con validación por campo, comando limitado a ProjectID y cuatro entradas, generador ID, puerto `Create(ctx, story) error` y `ErrProjectNotFound` desacoplado de pgx. Reejecutar `go test ./...` tras GREEN; agregar primero un borde RED (p. ej. criterios Unicode), resolverlo y simplificar sin ampliar contrato, dejando el runner verde tras cada etapa final. No introducir lectura previa ni aceptar ID/estado/puntos del cliente.
- [ ] 5. **RED:** crear `tests/integration/story/postgres/repository_integration_test.go` con Testcontainers y migraciones `000001` + `000002` (referencia: `tests/integration/project/postgres/repository_integration_test.go`): proyecto presente/faltante, fila y orden `TEXT[]`, `story_points IS NULL`, `23503` de FK nombrada frente a otro `23503`/error inesperado, rechazo de borrado de proyecto referenciado y órdenes controlados inserción/borrado sin huérfanos. Ejecutar `go test ./...` y registrar RED; Docker requerido, skip no acredita RED de persistencia.
- [ ] 6. **GREEN → TRIANGULATE → REFACTOR:** añadir `internal/project/infrastructure/postgres/migrations/000002_create_stories.{up,down}.sql` y `internal/story/infrastructure/postgres/repository.go`: PK/FK nombrada `stories_project_id_fkey`, checks básicos, `story_points INTEGER NULL` (US-14 podrá actualizarlo), INSERT único parametrizado sin SELECT, traducir solo `23503` de esta FK a `ErrProjectNotFound`; no reintentar resultado de red incierto. Ejecutar `go test ./...` tras GREEN, añadir borde RED de integridad y resolverlo, simplificar, y al cerrar ejecutar runner y comando de integración con Docker **sin skips**. Si la carrera o Testcontainers elevan el diff hacia 400, separar un borde de integridad en otro PR interno completo antes del endpoint, no omitir pruebas.
- [ ] 7. **RED:** crear `tests/unit/story/transport/http/handler_test.go` con `PathValue` en requests directas y casos `201` (ID/ProjectID UUID, `pendiente`, prioridad, criterios ordenados, clave `story_points:null`), `400 invalid_request` fijo para JSON mal formado, múltiples valores, tipos, cuerpo `null`/array y claves extra incluidas `id`, `project_id`, `status`, `story_points` aun con null/0; `422 validation_failed` para UUID y entradas inválidas sin escritura, `404` solo error tipado y `500` genérico sin secretos; ejecutar `go test ./...` y registrar RED.
- [ ] 8. **GREEN → TRIANGULATE → REFACTOR:** implementar `internal/story/transport/http/handler.go` con objeto JSON único, `DisallowUnknownFields`, UUID normalizado antes de DB, mapeos seguros 400/422/404/500 y `story_points` nulo sin `omitempty`; ejecutar `go test ./...` tras GREEN, añadir primero un caso RED de rechazo (p. ej. Unicode/prioridad con mayúsculas), resolverlo y refactorizar con runner verde. No registrar la ruta en `internal/api/api.go` en este corte.
- [ ] 9. **RED:** ampliar `tests/unit/cmd/api/main_test.go` para registro de nueva ruta/método no creador y preservación `POST /projects`; agregar prueba HTTP real a `tests/integration/story/postgres/repository_integration_test.go` (o archivo concreto `tests/integration/story/postgres/http_integration_test.go`) con migraciones aplicadas, alta y proyecto inexistente. Ejecutar `go test ./...` y registrar RED con Docker; no acreditar integración omitida.
- [ ] 10. **GREEN → TRIANGULATE → REFACTOR:** adaptar `internal/api/api.go` y `cmd/api/main.go` para inyectar repo PostgreSQL/generador UUID, registrar ruta solo tras disponibilidad de 000002; actualizar `README.md` con request/response, cuatro campos, ProjectID conocido, comando `migrate ... up` previo al servidor y advertencia sobre rollback con datos. Ejecutar `go test ./...` tras GREEN; añadir un caso RED de composición/HTTP, resolverlo y refactorizar; ejecutar runner e integración Docker reales al cierre, comprobar diff ≤400 incluyendo README/tests y verificar manualmente alta/rechazo contra DB migrada. No modificar US-01 ni cerrar #33 antes de la verificación final.

## Corte 3 correctivo — solo RED

- Rama nueva `feat/us05-story-core-tdd` desde `9a4924b`; el intento previo `feat/us05-story-core` en `3908954` permanece intacto como evidencia. Corte encadenado `stacked-to-main`, sin excepción de tamaño ni push/PR/merge. Solo RED autorizado; detenerse antes de GREEN.
- Tarea 3 completada: casilla `- [x] 3` en `tasks.md`. Dos pruebas unitarias nuevas: `tests/unit/story/domain/story_test.go` y `tests/unit/story/application/create_story_test.go`. API diseñada: `domain.NewStory(id, projectID, title, description, priority, criteria) (Story, error)`, `domain.ValidationError.Fields`, `application.CreateStoryCommand` (ProjectID, Title, Description, Priority, AcceptanceCriteria), `NewCreateStoryUseCase(repo, generateID).Execute(ctx, command)`, `ErrProjectNotFound` y puerto `Create(ctx, domain.Story) error`.
- `go test ./...`: **FAIL (exit 1)**. Setup failed para `tests/unit/story/application` y `tests/unit/story/domain`: `no required module provides package .../internal/story/application` y `.../internal/story/domain` respectivamente; los paquetes existentes reportaron `ok` o `no test files`. Fallo esperado por módulos aún inexistentes, no se afirma que las aserciones hayan corrido. Runtime harness: N/A, no existe comportamiento de historias ejecutable. Sin código de producción ni desviaciones de diseño.
- TDD Cycle Evidence: | Tarea | RED | GREEN | TRIANGULATE | REFACTOR |
  | --- | --- | --- | --- | --- |
  | 3 | Tests escritos; runner falla por imports ausentes (exit 1) | Pendiente | Pendiente: agregar primero borde Unicode en criterios | Pendiente |
- Estado nativo consumido: `gentle-ai.sdd-status` v2, `applyState: ready`, `nextRecommended: apply`, 2/10 completas antes del RED, `blockedReasons: []`, `notes: []`; `actionContext: repo-local`, `workspaceRoot` y `allowedEditRoots` en la raíz de este repositorio, sin advertencias. Estado nativo posterior no consultado.
- Pendientes: las líneas exactas `- [ ] 4.` a `- [ ] 10.` están en `tasks.md` y en la sección histórica anterior; la línea `- [ ] 3.` anterior refleja solo el corte 2, no el estado actual. Reversión del RED: retirar únicamente ambas pruebas y revertir esta entrada y casilla 3; ningún dato ni US-01 afectado.

## Corte 3 correctivo — solo GREEN

- RED previo: commit `2f4893a9b7f31264c9025111ad78c2ef1f440aca`, falló `go test ./...` por paquetes de historias ausentes; pruebas intactas. Intento correctivo, no continuación ni copia del intento anterior `feat/us05-story-core` (rama preservada).
- GREEN: nuevos `internal/story/domain/story.go` e `internal/story/application/create_story.go`; validación por campo, textos y orden conservados, puntos nulos, estado pendiente, ID inyectado, una escritura válida y error tipado independiente de PostgreSQL. Sin ruta HTTP ni migración. Tarea 3 ya `[x]`; tarea 4 continúa `[ ]` hasta TRIANGULATE y REFACTOR. Casillas 5–10 siguen sin marcar; no se modificó `tasks.md` en este paso.
- Pruebas: `go test -count=1 ./tests/unit/story/...` PASS (ambos paquetes); `go test ./...` PASS (integración de proyectos cacheada); `go test -count=1 -v ./tests/integration/...` PASS con **2 SKIP** por acceso denegado a Docker, no acredita PostgreSQL. Runtime harness: N/A, módulo interno sin composición ni endpoint público.
### TDD Cycle Evidence

| Tarea | RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- |
| 4 (parcial) | Tests de commit `2f4893a` fallaron por imports inexistentes | Tests unitarios focalizados y runner verde | Pendiente, sin prueba nueva | Pendiente |
- Sin desviaciones del diseño. Reversión del GREEN: retirar solo ambos archivos de producción y esta sección, conservando RED y US-01. Límite PR 3 `stacked-to-main`, base `9a4924b`, ≤400 líneas del corte incluyendo pruebas y documentación; sin excepción, push, PR ni merge. Estado nativo consumido: `gentle-ai.sdd-status` v2, `applyState: ready`, `nextRecommended: apply`, 3/10 tareas, sin bloqueos; `actionContext: repo-local` y raíz autorizada del repositorio, sin advertencias. No se obtuvo estado nativo posterior.
- Pendientes: las líneas exactas `- [ ] 4.` a `- [ ] 10.` permanecen en `tasks.md`; TRIANGULATE/REFACTOR de tarea 4 no se iniciaron.

## Corte 3 correctivo — TRIANGULATE (RED observado; detenerse)

- Base `673be0c` tras RED `2f4893a` y GREEN `673be0c`; tests existentes antes de editar: `go test -count=1 ./tests/unit/story/...` PASS (2 paquetes). Solo pruebas nuevas en `tests/unit/story/{domain/story_test.go,application/create_story_test.go}` y este progreso; fuentes y casillas intactas.
- Borde añadido: criterio compuesto por espacios Unicode (`U+2003` y `U+00A0`) se rechaza; en aplicación, entrada inválida no debe consumir un ID ni escribir. El dominio pasó y la aplicación falló porque genera un ID **una vez** antes de validar, aunque hace cero escrituras. Es un RED conductual real, no un fallo de infraestructura; no modificar fuentes, esperar decisión del parent para GREEN.
- `go test -count=1 ./tests/unit/story/...`: FAIL exit 1; `TestCreateStoryDoesNotGenerateIDForInvalidInput`: `generated IDs/writes = 1/0, want zero before valid input`; paquete domain PASS. `go test ./...`: FAIL exit 1 con el mismo test, resto PASS/no test files; integración de proyectos cacheada, sin prueba nueva de PostgreSQL. Runtime harness: N/A (núcleo interno sin ruta HTTP).
### TDD Cycle Evidence

| Tarea | Safety net | RED previo | GREEN previo | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- | --- |
| 4 (parcial) | Unitarios de historias PASS antes de editar | `2f4893a` (imports ausentes) | `673be0c` (unitarios y runner PASS) | Nuevo borde Unicode: domain PASS; generación prematura: RED observado en ambos comandos | Pendiente; prohibido en este corte |

- Tarea 3 ya `[x]`; tarea 4 sigue `[ ]` sin GREEN de este borde ni REFACTOR; tareas 5–10 siguen `[ ]`. Las líneas exactas pendientes `- [ ] 4.` a `- [ ] 10.` figuran en la sección «Corte 2 — plan operativo y evidencia» y permanecen sin marcar en `tasks.md`. Sin desviación del contrato de creación; generación anticipada es el hallazgo. Reversión: retirar solo los casos nuevos y esta sección, conservando RED/GREEN previos. PR 3 `stacked-to-main`, base `9a4924b`, tope 400, sin excepción ni publicación.
- Estado nativo consumido: `gentle-ai.sdd-status` v2, `applyState: ready`, `nextRecommended: apply`, 3/10 tareas completas; `blockedReasons: []`; `actionContext: repo-local`, raíz y allowedEditRoots del repositorio, sin advertencias. Estado posterior no consultado. Detenerse tras commit de pruebas distinto, sin dar por terminada tarea 4.

## Corte 3 correctivo — TRIANGULATE GREEN (sin REFACTOR)

- RED `e163c23` confirmado: `go test -count=1 ./tests/unit/story/...` y `go test ./...` FAIL exit 1 en `TestCreateStoryDoesNotGenerateIDForInvalidInput` (ID/escrituras `1/0`, esperado `0/0`); dominio PASS. Sin cambios de tests.
- GREEN: `internal/story/application/create_story.go` valida mediante `NewStory` antes de generar ID; una escritura solo después de validación. Ambos comandos anteriores PASS (unitarios de historias: 2 paquetes); integración de proyectos en `go test ./...` cacheada, sin nueva prueba de DB. Harness runtime: N/A, núcleo sin endpoint. Desviaciones: ninguna.
- TDD Cycle Evidence: | Tarea | RED | GREEN | TRIANGULATE | REFACTOR |
  | --- | --- | --- | --- | --- |
  | 4 (parcial) | `e163c23` falla en generación anticipada | Validación antes de ID; ambos runners PASS | Borde Unicode satisfecho; fuente corregida | Pendiente: detenerse antes |
- Casillas persistidas al cierre de este paso histórico: 3 `[x]`; 4–10 `[ ]` (líneas exactas en sección «Corte 2» y `tasks.md`); no marcar 4 hasta REFACTOR. PR 3 `stacked-to-main`, base `9a4924b`, ≤400 líneas sin excepción. Reversión: revertir solo esta corrección y evidencia, conservando RED y GREEN previos. Estado nativo v2 consumido: apply ready, 3/10, next apply; `actionContext: repo-local` con root autorizado, sin advertencias; estado posterior no consultado.

## Corte 3 correctivo — REFACTOR evaluado, sin cambio de código

- Revisados `internal/story/{domain/story.go,application/create_story.go}` y ambos tests unitarios: validación por campo y `TrimSpace` Unicode, copia de criterios, validación antes de ID, una escritura por comando válido y errores propagados son directos. No hay simplificación justificada que conserve el comportamiento y mejore la legibilidad; no fabricar cambios en fuente o pruebas. Sin desviaciones del diseño. Se marca tarea 4 `[x]` en `tasks.md` tras comprobar el contrato y runners; 5–10 permanecen `[ ]`.
- Evidencia cronológica TDD de tarea 4: RED `2f4893a` (imports ausentes) → GREEN `673be0c` (runner verde) → TRIANGULATE RED `e163c23` (ID consumido para criterio Unicode inválido) → GREEN `89e93fd` (validar antes de generar ID) → REFACTOR: revisión sin modificación útil, tests verdes. Este commit de documentación registra el cierre, separado de los anteriores.
### TDD Cycle Evidence — cierre tarea 4

| RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- |
| `2f4893a`, imports ausentes | `673be0c`, runner PASS | `e163c23` RED ID prematuro → `89e93fd` GREEN | Revisión sin cambio útil; runners PASS |

- `go test -count=1 ./tests/unit/story/...`: PASS (2 paquetes). `go test -count=1 ./...`: PASS; integración de proyectos no acreditada: `go test -count=1 -v ./tests/integration/...` PASS con 2 SKIP por permiso denegado en Docker. `git diff --check`: PASS tras la edición. Harness runtime de historias: N/A, núcleo no registrado en API; no SQL/HTTP. Rollback del corte completo: revertir solo fuentes y tests de story y las casillas/evidencia del corte 3, sin US-01 ni datos.
- Base real PR 3: `9a4924bb` en rama hija `feat/us05-story-core-tdd`, PR 2 no integrado en `main`; corregida la descripción del parent en `tasks.md`. `stacked-to-main`, sin excepción; diff aislado del corte debe quedar ≤400 adiciones + eliminaciones incluidas estas evidencias. No push, PR, merge, rebase ni eliminación de ramas. Detenerse aquí; PR 4 no iniciado.
- Estado nativo consumido: `gentle-ai.sdd-status` v2, `applyState: ready`, `nextRecommended: apply`, 3/10 antes de marcar 4, `blockedReasons: []`; `actionContext: repo-local`, raíz canónica y `allowedEditRoots` en este repositorio, sin advertencias. No se consultó estado nativo posterior; quedan tareas 5–10 pendientes.
- Líneas exactas aún sin marcar en `tasks.md`:

- [ ] 5. **RED:** crear `tests/integration/story/postgres/repository_integration_test.go` con Testcontainers y migraciones `000001` + `000002` (referencia: `tests/integration/project/postgres/repository_integration_test.go`): proyecto presente/faltante, fila y orden `TEXT[]`, `story_points IS NULL`, `23503` de FK nombrada frente a otro `23503`/error inesperado, rechazo de borrado de proyecto referenciado y órdenes controlados inserción/borrado sin huérfanos. Ejecutar `go test ./...` y registrar RED; Docker requerido, skip no acredita RED de persistencia.
- [ ] 6. **GREEN → TRIANGULATE → REFACTOR:** añadir `internal/project/infrastructure/postgres/migrations/000002_create_stories.{up,down}.sql` y `internal/story/infrastructure/postgres/repository.go`: PK/FK nombrada `stories_project_id_fkey`, checks básicos, `story_points INTEGER NULL` (US-14 podrá actualizarlo), INSERT único parametrizado sin SELECT, traducir solo `23503` de esta FK a `ErrProjectNotFound`; no reintentar resultado de red incierto. Ejecutar `go test ./...` tras GREEN, añadir borde RED de integridad y resolverlo, simplificar, y al cerrar ejecutar runner y comando de integración con Docker **sin skips**. Si la carrera o Testcontainers elevan el diff hacia 400, separar un borde de integridad en otro PR interno completo antes del endpoint, no omitir pruebas.
- [ ] 7. **RED:** crear `tests/unit/story/transport/http/handler_test.go` con `PathValue` en requests directas y casos `201` (ID/ProjectID UUID, `pendiente`, prioridad, criterios ordenados, clave `story_points:null`), `400 invalid_request` fijo para JSON mal formado, múltiples valores, tipos, cuerpo `null`/array y claves extra incluidas `id`, `project_id`, `status`, `story_points` aun con null/0; `422 validation_failed` para UUID y entradas inválidas sin escritura, `404` solo error tipado y `500` genérico sin secretos; ejecutar `go test ./...` y registrar RED.
- [ ] 8. **GREEN → TRIANGULATE → REFACTOR:** implementar `internal/story/transport/http/handler.go` con objeto JSON único, `DisallowUnknownFields`, UUID normalizado antes de DB, mapeos seguros 400/422/404/500 y `story_points` nulo sin `omitempty`; ejecutar `go test ./...` tras GREEN, añadir primero un caso RED de rechazo (p. ej. Unicode/prioridad con mayúsculas), resolverlo y refactorizar con runner verde. No registrar la ruta en `internal/api/api.go` en este corte.
- [ ] 9. **RED:** ampliar `tests/unit/cmd/api/main_test.go` para registro de nueva ruta/método no creador y preservación `POST /projects`; agregar prueba HTTP real a `tests/integration/story/postgres/repository_integration_test.go` (o archivo concreto `tests/integration/story/postgres/http_integration_test.go`) con migraciones aplicadas, alta y proyecto inexistente. Ejecutar `go test ./...` y registrar RED con Docker; no acreditar integración omitida.
- [ ] 10. **GREEN → TRIANGULATE → REFACTOR:** adaptar `internal/api/api.go` y `cmd/api/main.go` para inyectar repo PostgreSQL/generador UUID, registrar ruta solo tras disponibilidad de 000002; actualizar `README.md` con request/response, cuatro campos, ProjectID conocido, comando `migrate ... up` previo al servidor y advertencia sobre rollback con datos. Ejecutar `go test ./...` tras GREEN; añadir un caso RED de composición/HTTP, resolverlo y refactorizar; ejecutar runner e integración Docker reales al cierre, comprobar diff ≤400 incluyendo README/tests y verificar manualmente alta/rechazo contra DB migrada. No modificar US-01 ni cerrar #33 antes de la verificación final.

## Corte 4 — RED inicial, detenerse antes de GREEN

- Estado nativo consumido: `gentle-ai.sdd-status` v2, cambio `us-05-crear-historia-product-backlog`, almacén `openspec`, `applyState: ready`, `nextRecommended: apply`, 4/10 completas antes del corte; `blockedReasons: []`, `notes: []`. `actionContext: repo-local`, `workspaceRoot` y único `allowedEditRoots` igual a la raíz canónica del repositorio; sin advertencias. No se consultó estado posterior.
- Base real limpia `9321106cb4c727dc6c2fd8a17bf0df66385348ad` (`feat/us05-story-core-tdd`); rama hija nueva `feat/us05-story-storage`, no `main`, sin integración previa de los cortes. `stacked-to-main`; el usuario rechazó dividir el corte 4 y aprobó **solo para este corte** `delivery_strategy=exception-ok` / `size:exception` (pronóstico 430–600), conservando 400 para los otros cortes. No push, PR, merge, rebase ni borrado. Límite de PR: almacenamiento y FK sin HTTP; este commit es solo RED y no está listo para integrar.
- Tarea 5 `[x]` marcada en `tasks.md` después de escribir la prueba. Archivo nuevo: `tests/integration/story/postgres/repository_integration_test.go`; sin fuente, migración ni SQL de producción. Pruebas Testcontainers prevén ambas migraciones, fila real con criterios ordenados y `story_points IS NULL`, FK nombrada/ausencia de proyecto, PK y otro `23503` sin falsa traducción, restricción de borrado y ambos órdenes controlados con recuento explícito de huérfanos. La concurrencia real queda como borde adicional honesto para TRIANGULATE de tarea 6; las aserciones nuevas aún no se han ejecutado.
- `gofmt -w tests/integration/story/postgres/repository_integration_test.go`; `go test ./...` **FAIL exit 1**: setup de `tests/integration/story/postgres`, `no required module provides package .../internal/story/infrastructure/postgres`. Los paquetes previos aparecen PASS/cacheados o sin tests; el fallo es el paquete aún no implementado, no sintaxis ni Docker. No se acredita que estas aserciones, Testcontainers o las migraciones hayan corrido. Runtime harness: N/A, repositorio de historias aún no existe; runner de integración Docker queda pendiente tras GREEN.
- Tamaño authored provisional del RED: +318/−1 = 319 líneas (289 de test nuevo, 26 de progreso y 4 de tareas, contando archivo sin seguimiento); excepción de tamaño solo del corte 4, no una obligación de comprimir pruebas.
- Desviaciones del diseño: ninguna. Reversión de este RED: revertir solo prueba nueva, casilla 5 y esta entrada/decisión acotada; sin datos, proyectos ni trabajo de PR 3 afectados. Estado del corte: detenerse después del commit RED; GREEN, TRIANGULATE y REFACTOR prohibidos en este lanzamiento.

### TDD Cycle Evidence — tarea 5

| Tarea | Archivo | Capa | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 5 | `tests/integration/story/postgres/repository_integration_test.go` | PostgreSQL/Testcontainers | N/A, archivo nuevo; baseline previo informado PASS sin skips | `go test ./...` exit 1, import de repo ausente; aserciones no ejecutadas | Pendiente | Pendiente: caso de carrera real | Pendiente |

- Pruebas escritas: 4 funciones superiores, una con dos subcasos; pruebas nuevas passing: 0 (sin compilación). No refactor, funciones puras ni approval tests en este paso.
- Pendientes exactos en `tasks.md` al cierre: las líneas `- [ ] 6.` a `- [ ] 10.` reproducidas en la sección «Corte 3 correctivo — REFACTOR evaluado, sin cambio de código» permanecen sin marcar; la antigua `- [ ] 5.` de esa sección es evidencia histórica, no estado actual.

Líneas exactas pendientes actuales de `tasks.md`:

- [ ] 6. **GREEN → TRIANGULATE → REFACTOR:** añadir `internal/project/infrastructure/postgres/migrations/000002_create_stories.{up,down}.sql` y `internal/story/infrastructure/postgres/repository.go`: PK/FK nombrada `stories_project_id_fkey`, checks básicos, `story_points INTEGER NULL` (US-14 podrá actualizarlo), INSERT único parametrizado sin SELECT, traducir solo `23503` de esta FK a `ErrProjectNotFound`; no reintentar resultado de red incierto. Ejecutar `go test ./...` tras GREEN, añadir borde RED de integridad y resolverlo, simplificar, y al cerrar ejecutar runner y comando de integración con Docker **sin skips**. Si la carrera o Testcontainers elevan el diff hacia 400, separar un borde de integridad en otro PR interno completo antes del endpoint, no omitir pruebas.
- [ ] 7. **RED:** crear `tests/unit/story/transport/http/handler_test.go` con `PathValue` en requests directas y casos `201` (ID/ProjectID UUID, `pendiente`, prioridad, criterios ordenados, clave `story_points:null`), `400 invalid_request` fijo para JSON mal formado, múltiples valores, tipos, cuerpo `null`/array y claves extra incluidas `id`, `project_id`, `status`, `story_points` aun con null/0; `422 validation_failed` para UUID y entradas inválidas sin escritura, `404` solo error tipado y `500` genérico sin secretos; ejecutar `go test ./...` y registrar RED.
- [ ] 8. **GREEN → TRIANGULATE → REFACTOR:** implementar `internal/story/transport/http/handler.go` con objeto JSON único, `DisallowUnknownFields`, UUID normalizado antes de DB, mapeos seguros 400/422/404/500 y `story_points` nulo sin `omitempty`; ejecutar `go test ./...` tras GREEN, añadir primero un caso RED de rechazo (p. ej. Unicode/prioridad con mayúsculas), resolverlo y refactorizar con runner verde. No registrar la ruta en `internal/api/api.go` en este corte.
- [ ] 9. **RED:** ampliar `tests/unit/cmd/api/main_test.go` para registro de nueva ruta/método no creador y preservación `POST /projects`; agregar prueba HTTP real a `tests/integration/story/postgres/repository_integration_test.go` (o archivo concreto `tests/integration/story/postgres/http_integration_test.go`) con migraciones aplicadas, alta y proyecto inexistente. Ejecutar `go test ./...` y registrar RED con Docker; no acreditar integración omitida.
- [ ] 10. **GREEN → TRIANGULATE → REFACTOR:** adaptar `internal/api/api.go` y `cmd/api/main.go` para inyectar repo PostgreSQL/generador UUID, registrar ruta solo tras disponibilidad de 000002; actualizar `README.md` con request/response, cuatro campos, ProjectID conocido, comando `migrate ... up` previo al servidor y advertencia sobre rollback con datos. Ejecutar `go test ./...` tras GREEN; añadir un caso RED de composición/HTTP, resolverlo y refactorizar; ejecutar runner e integración Docker reales al cierre, comprobar diff ≤400 incluyendo README/tests y verificar manualmente alta/rechazo contra DB migrada. No modificar US-01 ni cerrar #33 antes de la verificación final.

## Corte 4 — GREEN únicamente (sin TRIANGULATE ni REFACTOR)

- RED inmutable: commit `1dd8cc18c9fe732c87897b018e7185c2696cd61f`, hash del test `4a5a8797b4d14d7bc2cb870495adc825d75bcee6`; el parent confirmó `go test ./...` exit 1 únicamente por import ausente de `internal/story/infrastructure/postgres`. Ninguna aserción de ese test corrió durante RED; no se modificaron pruebas en GREEN.
- Archivos nuevos de GREEN: `internal/story/infrastructure/postgres/repository.go`, `internal/project/infrastructure/postgres/migrations/000002_create_stories.up.sql` y `000002_create_stories.down.sql`. Inserción parametrizada única sin SELECT ni reintento; solo SQLSTATE `23503` + constraint `stories_project_id_fkey` se traduce a `ErrProjectNotFound`. FK `ON DELETE RESTRICT`, checks de prioridad/criterios y puntos `INTEGER NULL`; down elimina solo `stories`, nunca `projects`.
- `go test ./...`: PASS (exit 0); paquete de historias integradas sin caché, resto de paquetes previos potencialmente cacheados. `go test -count=1 -v ./tests/integration/...`: PASS (exit 0), **sin SKIP**; Testcontainers PostgreSQL 16 real ejecutó dos escenarios de proyectos, creación persistida con criterios en orden y `story_points IS NULL`, ausencia de proyecto y FK nombrada, PK duplicada y otro `23503` preservados, eliminación restringida y ambos órdenes inserción/eliminación sin huérfanos. No se aplicó migración fuera de bases desechables. `git diff --check`: PASS. Runtime harness: PostgreSQL real; no hay endpoint HTTP aún.

### TDD Cycle Evidence — tarea 6 (parcial)

| RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- |
| `1dd8cc1` import ausente, test hash `4a5a8797` | Runner e integración real PASS, sin skips | Pendiente: comprobar borde concurrente genuino antes de agregar prueba | Pendiente |

- **Tarea 6 permanece `- [ ]` en `tasks.md`**, sin cambios de casillas en GREEN; tareas 7–10 tampoco iniciadas. Las líneas exactas pendientes 6–10 están en `tasks.md` y en el bloque «Líneas exactas pendientes actuales» anterior; no se duplican aquí.
- Diseño: sin desviaciones. Riesgo remanente: las pruebas actuales verifican dos órdenes secuenciales, no la intercalación concurrente; el resultado de red incierto tampoco promete ausencia de fila, solo integridad referencial. No fabricar RED futuro. Reversión de este GREEN: retirar exactamente las tres rutas nuevas, previa evaluación de datos si `stories` llegó a instalarse fuera del test; las pruebas RED y proyectos US-01 permanecen.
- Límite de entrega: slice 4 `feat/us05-story-storage`, base `9321106`, `stacked-to-main`; `size:exception` explícita solo aquí, sin partir ni comprimir código para caber en 400. Corte GREEN termina antes de TRIANGULATE; no push/PR/merge/rebase. Estado nativo v2 consumido: `applyState: ready`, 5/10 completadas, `nextRecommended: apply`, `blockedReasons: []`, `actionContext: repo-local`, raíz canónica y único allowedEditRoots el repositorio, sin advertencias. No se obtuvo estado nativo posterior.

## Corte 4 — TRIANGULATE test-first, GREEN-on-arrival; detenerse

- Base limpia `feat/us05-story-storage` `6f379ac`, RED `1dd8cc1` anterior y GREEN `6f379ac` confirmados por parent; sin cambios de producción, SQL ni comportamiento. Archivos de este paso: `tests/integration/story/postgres/repository_integration_test.go` y este progreso. Tarea 6 **sigue `- [ ]`** en `tasks.md` (las líneas pendientes exactas 6–10 ya figuran arriba); ninguna casilla nueva se marca ni se reclama cierre del corte.
- Safety net antes de editar: `go test -count=1 -v ./tests/integration/story/postgres` PASS exit 0, cuatro tests superiores existentes, dos subtests secuenciales, PostgreSQL real sin SKIP. Borde: transacción sin confirmar inserta historia o borra proyecto; segundo writer intenta DELETE o llama al repositorio `Create`. Solo liberar el primer commit después de observar al segundo esperando un lock en `pg_stat_activity`; timeout de 10s, ticker de 20ms solo para sondear el estado, resultado de goroutine por canal. Verificar error FK `23503` nombrado si gana inserción, `ErrProjectNotFound` si gana borrado, recuentos y cero huérfanos en ambos casos.
- Primer intento `go test -count=1 -v ./tests/integration/story/postgres` FAIL exit 1: `delete commits first` esperó 10s y agotó timeout. Defecto **del test**, no RED de producto: `ltrim(query)` en PostgreSQL elimina espacios, no tabulaciones/salto de línea del SQL multilínea del repositorio; el observador no reconoció el writer bloqueado. Se corrigió **solo la observación de test** usando `position($1 in query) > 0`, sin tocar fuente/SQL. Reejecución del mismo comando PASS exit 0: ambos subtests concurrentes PASS y ningún SKIP. `go test ./...` PASS exit 0 (integración story ejecutada, resto parcialmente cacheado); `git diff --check` PASS exit 0 antes de este registro.

### TDD Cycle Evidence — tarea 6 (TRIANGULATE parcial)

| Safety net | RED previo | GREEN previo | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- |
| Integración story real PASS antes de editar | `1dd8cc1`: import ausente; no era carrera | `6f379ac`: integración real PASS | Caso concurrente real con ambos órdenes **GREEN-on-arrival** tras corregir observador de test; **no se observó nuevo RED de producto** | Pendiente de decisión del parent; no se inició |

- Verificación runtime: Testcontainers PostgreSQL 16 real; no HTTP público ni harness adicional aplicable. Desviación de diseño: ninguna; FK ya garantiza integridad concurrente y la prueba no requiere cambio de producto. Reversión: retirar únicamente el nuevo test/helper y esta entrada, conservando migración/repo y evidencia anterior. Boundary PR 4: `stacked-to-main`, base `9321106`, excepción `size:exception` explícita únicamente para slice4; commit de pruebas/evidencia separado, sin push/PR/merge/rebase. Estado nativo consumido: `gentle-ai.sdd-status` v2, `applyState: ready`, 5/10 completas, `nextRecommended: apply`, `blockedReasons: []`, `actionContext: repo-local` con `workspaceRoot` y `allowedEditRoots` iguales a raíz canónica, sin advertencias. Estado posterior no solicitado; detenerse para decisión del parent, sin fabricar RED ni marcar tarea 6.

## Corte 4 — cierre de tarea 6: REFACTOR evaluado sin cambio de fuente

- Decisión explícita del usuario: documentar el TRIANGULATE **GREEN-on-arrival** y continuar con revisión/refactor y verificación final solo de slice4. No se inventó un RED conductual ni se alteró la historia: RED `1dd8cc1` (import ausente, sin aserciones ejecutadas), GREEN `6f379ac`, TRIANGULATE `6b3441a` (test concurrente verde tras corregir observador de test). La primera falla de TRIANGULATE fue exclusivamente del observador del test, no del producto.
- REFACTOR: revisados `internal/story/infrastructure/postgres/repository.go`, `internal/project/infrastructure/postgres/migrations/000002_create_stories.{up,down}.sql` y `tests/integration/story/postgres/repository_integration_test.go`. La inserción única, traducción de FK por código y nombre, restricción de borrado y prueba de bloqueo concurrente son legibles y acotadas; no se justifica modificar producción ni pruebas por estética. Desviación acotada frente a la secuencia ideal: el borde concurrente no produjo RED de producto; el usuario aprobó registrar el GREEN-on-arrival, no fabricar uno. No se amplía contrato ni se inicia HTTP.
- `go test ./...`: PASS exit 0; integración de historias y proyectos aparece cacheada en este comando, por lo que no se usa para acreditar una ejecución nueva de PostgreSQL. `go test -count=1 -v ./tests/integration/...`: PASS exit 0, PostgreSQL Testcontainers real; 2 tests superiores de proyectos y 5 de historias (incluidos dos subtests de órdenes secuenciales y dos concurrentes) PASS, **0 SKIP**. `git diff --check`: PASS. Solo se cambian `tasks.md` y este progreso en el cierre; fuentes, SQL y tests existentes intactos.
- Tarea 6 marcada `- [x]` en `tasks.md` tras los checks; tareas 7–10 continúan `- [ ]`. Tarea 5 permanece `[x]`. Sin commits, ramas nuevas, HTTP, push, PR ni merge. `.pi/` sin seguimiento preservado.

### TDD Cycle Evidence — cierre tarea 6

| RED histórico | GREEN histórico | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- |
| `1dd8cc1`: import ausente; no aserciones de DB | `6f379ac`: runner e integración real PASS sin SKIP | `6b3441a`: carrera real en ambos órdenes GREEN-on-arrival; test observador corregido, sin RED conductual de producto (excepción aprobada) | Revisión sin cambio útil; runner PASS e integración fresca PASS sin SKIP |

- Límite de entrega: PR 4 / slice4 `feat/us05-story-storage`, base real `9321106cb4c727dc6c2fd8a17bf0df66385348ad`; `stacked-to-main` depende de los cortes previos y no se publicó. `size:exception` explícita únicamente para slice4 (pronóstico 430–600); los demás mantienen 400. Reversión: evaluar datos antes de ejecutar down; este elimina `stories` solamente. Diff aislado medido contra esa base tras esta entrada: +530/−2 = **532 líneas** (fuente, SQL, pruebas y SDD); excede 400 bajo la excepción aprobada para slice4. No comprimir fuente, comentarios ni tests por presupuesto.
- Estado nativo consumido: `gentle-ai.sdd-status` v2, `applyState: ready`, `nextRecommended: apply`, 5/10 previas, `blockedReasons: []`, `notes: []`; `actionContext: repo-local`, `workspaceRoot` y `allowedEditRoots` iguales a la raíz canónica del repositorio, sin advertencias. Estado nativo posterior no solicitado; con 6/10 casillas completas aún quedan 7–10 y no corresponde recomendar archive.
- Pendientes exactos actuales de `tasks.md`:

- [ ] 7. **RED:** crear `tests/unit/story/transport/http/handler_test.go` con `PathValue` en requests directas y casos `201` (ID/ProjectID UUID, `pendiente`, prioridad, criterios ordenados, clave `story_points:null`), `400 invalid_request` fijo para JSON mal formado, múltiples valores, tipos, cuerpo `null`/array y claves extra incluidas `id`, `project_id`, `status`, `story_points` aun con null/0; `422 validation_failed` para UUID y entradas inválidas sin escritura, `404` solo error tipado y `500` genérico sin secretos; ejecutar `go test ./...` y registrar RED.
- [ ] 8. **GREEN → TRIANGULATE → REFACTOR:** implementar `internal/story/transport/http/handler.go` con objeto JSON único, `DisallowUnknownFields`, UUID normalizado antes de DB, mapeos seguros 400/422/404/500 y `story_points` nulo sin `omitempty`; ejecutar `go test ./...` tras GREEN, añadir primero un caso RED de rechazo (p. ej. Unicode/prioridad con mayúsculas), resolverlo y refactorizar con runner verde. No registrar la ruta en `internal/api/api.go` en este corte.
- [ ] 9. **RED:** ampliar `tests/unit/cmd/api/main_test.go` para registro de nueva ruta/método no creador y preservación `POST /projects`; agregar prueba HTTP real a `tests/integration/story/postgres/repository_integration_test.go` (o archivo concreto `tests/integration/story/postgres/http_integration_test.go`) con migraciones aplicadas, alta y proyecto inexistente. Ejecutar `go test ./...` y registrar RED con Docker; no acreditar integración omitida.
- [ ] 10. **GREEN → TRIANGULATE → REFACTOR:** adaptar `internal/api/api.go` y `cmd/api/main.go` para inyectar repo PostgreSQL/generador UUID, registrar ruta solo tras disponibilidad de 000002; actualizar `README.md` con request/response, cuatro campos, ProjectID conocido, comando `migrate ... up` previo al servidor y advertencia sobre rollback con datos. Ejecutar `go test ./...` tras GREEN; añadir un caso RED de composición/HTTP, resolverlo y refactorizar; ejecutar runner e integración Docker reales al cierre, comprobar diff ≤400 incluyendo README/tests y verificar manualmente alta/rechazo contra DB migrada. No modificar US-01 ni cerrar #33 antes de la verificación final.

## Corte 5 — tarea 7 RED; detenerse antes de GREEN

- Base local real `feat/us05-story-handler` en `7f9eb80` (cierre del corte 4 en `feat/us05-story-storage`); los cortes previos todavía no están integrados en `main`. Decisión posterior del usuario: desarrollo/pruebas locales de #33 autorizados; integración previa a `main` se exige solo antes de publicar/desplegar. Estrategia `stacked-to-main`; PR 5 aislada, sin registro de ruta, publicación, merge ni excepción de tamaño.
- Tarea 7 marcada `[x]` en `tasks.md` tras RED observado. Nuevo `tests/unit/story/transport/http/handler_test.go`: handler directo con `PathValue`, doble de repositorio, éxito y respuesta con puntos nulos, forma JSON estricta, entradas inválidas sin escritura y errores 404/500 sin secretos. API propuesta: `transporthttp.NewCreateStoryHandler(*application.CreateStoryUseCase) http.Handler`; no hay código de producción.
- `go test ./...`: FAIL exit 1; setup del paquete de tests HTTP falla por import ausente `internal/story/transport/http` (`no required module provides package`). Las aserciones nuevas no se ejecutaron; paquetes existentes PASS/cacheados o sin pruebas, no se acredita integración fresca. `gofmt` aplicado a tests. Sin desviaciones del diseño. Reversión: retirar solo test, casilla y este registro.

### TDD Cycle Evidence — tarea 7

| RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- |
| Runner exit 1 por handler aún ausente; aserciones no ejecutadas | Pendiente: parent gate | Pendiente | Pendiente |

- Tamaño provisional contra base `7f9eb80`: test nuevo 165 líneas; tareas +3/−1; este registro ~19 líneas: ~188 líneas authored. Pronóstico honesto del corte completo: ~320–380 adiciones + eliminaciones si el handler y evidencia restantes ocupan ~130–190 líneas; medir antes de GREEN y detenerse si supera 400, sin comprimir tests ni fuente. PR 5 depende de PR 4; después sigue PR 6, sin despliegue ni ruta hasta migración disponible.
- Estado nativo consumido: `gentle-ai.sdd-status` v2, `applyState: ready`, 6/10 antes de RED, `nextRecommended: apply`, `blockedReasons: []`, `notes: []`; `actionContext: repo-local`, workspaceRoot y allowedEditRoots la raíz canónica de este repositorio, sin advertencias. Estado posterior no consultado.
- Pendientes exactos actuales en `tasks.md` (la lista histórica 7–10 anterior no refleja esta actualización):

- [ ] 8. **GREEN → TRIANGULATE → REFACTOR:** implementar `internal/story/transport/http/handler.go` con objeto JSON único, `DisallowUnknownFields`, UUID normalizado antes de DB, mapeos seguros 400/422/404/500 y `story_points` nulo sin `omitempty`; ejecutar `go test ./...` tras GREEN, añadir primero un caso RED de rechazo (p. ej. Unicode/prioridad con mayúsculas), resolverlo y refactorizar con runner verde. No registrar la ruta en `internal/api/api.go` en este corte.
- [ ] 9. **RED:** ampliar `tests/unit/cmd/api/main_test.go` para registro de nueva ruta/método no creador y preservación `POST /projects`; agregar prueba HTTP real a `tests/integration/story/postgres/repository_integration_test.go` (o archivo concreto `tests/integration/story/postgres/http_integration_test.go`) con migraciones aplicadas, alta y proyecto inexistente. Ejecutar `go test ./...` y registrar RED con Docker; no acreditar integración omitida.
- [ ] 10. **GREEN → TRIANGULATE → REFACTOR:** adaptar `internal/api/api.go` y `cmd/api/main.go` para inyectar repo PostgreSQL/generador UUID, registrar ruta solo tras disponibilidad de 000002; actualizar `README.md` con request/response, cuatro campos, ProjectID conocido, comando `migrate ... up` previo al servidor y advertencia sobre rollback con datos. Ejecutar `go test ./...` tras GREEN; añadir un caso RED de composición/HTTP, resolverlo y refactorizar; ejecutar runner e integración Docker reales al cierre, comprobar diff ≤400 incluyendo README/tests y verificar manualmente alta/rechazo contra DB migrada. No modificar US-01 ni cerrar #33 antes de la verificación final.

## Corte 5 — tarea 8 GREEN únicamente

- RED previo `6d2a2c0`: import ausente de `internal/story/transport/http`, sin aserciones ejecutadas. Nuevo `internal/story/transport/http/handler.go`: handler directo, JSON objeto único con campos permitidos, UUID normalizado, errores seguros 400/422/404/500, respuesta 201 con `story_points:null`; sin registrar ruta ni modificar pruebas. `tasks.md`: 7 `[x]`, 8–10 `[ ]`; TRIANGULATE y REFACTOR pendientes.
- `go test -count=1 ./tests/unit/story/transport/http`: PASS exit 0 (casos nuevos ejecutados); `go test ./...`: PASS exit 0 (integración PostgreSQL cacheada, no se acredita ejecución fresca). `git diff --check`: PASS. TDD Cycle Evidence: RED `6d2a2c0` import ausente → GREEN tests focalizados y runner PASS → TRIANGULATE pendiente → REFACTOR pendiente.
- Borde futuro candidato, todavía **no** probado: una clave JSON `title` duplicada con valores diferentes; decidir el contrato y agregar test primero en TRIANGULATE. Revisión de refactor pendiente tras ese borde; no se hizo cambio estético. Sin desviación de diseño. Reversión: retirar handler y esta entrada, conservar RED.
- PR 5 `stacked-to-main`, base real `7f9eb80`, sin publicación. Medición contra base: archivos versionados +196/−1; handler sin seguimiento +129; total **326 líneas** cambiadas; margen 74 hasta 400 para TRIANGULATE/REFACTOR/evidencia. `.pi/` sin seguimiento excluida porque no es parte de slice5 y permanece intacta.
- Estado nativo v2 consumido: `applyState: ready`, 7/10, `nextRecommended: apply`, `blockedReasons: []`; `actionContext: repo-local`, workspaceRoot/allowedEditRoots la raíz canónica del repositorio, sin advertencias. No se consultó estado posterior. Pendientes exactos `- [ ] 8.`, `- [ ] 9.`, `- [ ] 10.` conservados en `tasks.md` y al final del registro RED precedente. Detenerse antes de TRIANGULATE; parent controla commit cronológico.

## Corte 5 — TRIANGULATE test-first, RED observado; detenerse

- GREEN previo confirmado en commit `1a92f72`; ningún cambio de producción. Borde derivado del contrato existente: `acceptance_criteria` debe ser array de strings; un elemento JSON `null` es tipo incompatible y requiere `400 invalid_request` fijo, no la validación semántica de criterio string vacío (`422`). Un caso nuevo en la tabla de forma JSON comprueba 400 y cero escrituras. No se adoptó política nueva para claves duplicadas.
- `go test -count=1 ./tests/unit/story/transport/http`: FAIL exit 1; `TestCreateStoryRejectsInvalidJSONShape/null_criterion_is_not_a_string` obtuvo **422**, esperado **400**, cuerpo `validation_failed` con `acceptance_criteria`; las demás pruebas focalizadas pasan. `go test ./...`: FAIL exit 1 por el mismo caso; paquetes previos PASS/cacheados o sin tests. RED conductual real: `encoding/json` deserializa `null` en elemento string como cadena vacía; el dominio la rechaza después como dato inválido. La corrección futura debe distinguir tipo JSON incompatible antes de invocar el caso de uso, sin cambiar el rechazo 422 de array ausente/nulo/vacío ni texto en blanco.
- TDD Cycle Evidence tarea 8: RED original `6d2a2c0` import ausente → GREEN `1a92f72` → TRIANGULATE **RED** por elemento null → GREEN del borde pendiente → REFACTOR pendiente. Casilla 8 sigue `- [ ]` (y 9–10 `- [ ]`) en `tasks.md`; sin ruta registrada, commit ni publicación. Diseño sin desviaciones; rollback de este paso: revertir solo caso nuevo y esta evidencia.
- PR 5 base `7f9eb80`, `stacked-to-main`, límite 400; diff medido +333/−1 = **334 líneas**, margen 66 para corrección/evidencia. No comprimir código ni pruebas para ajustarlo. Estado nativo v2 consumido: `applyState: ready`, 7/10, `nextRecommended: apply`, sin bloqueos; `actionContext: repo-local`, workspaceRoot y allowedEditRoots raíz del repositorio sin advertencias. Estado posterior no consultado.

## Corte 5 — TRIANGULATE GREEN, detenerse antes de REFACTOR

- RED `75e5f8d` confirmado por parent: elemento `null` en array de criterios obtuvo 422 en vez de 400. Solo `internal/story/transport/http/handler.go` cambió: tras decodificación estricta, inspecciona elementos JSON de `acceptance_criteria` cuando es array y rechaza `null` como tipo incompatible antes de invocar el caso de uso; array ausente/nulo/vacío y strings en blanco continúan como 422. Tests intactos; sin política sobre claves duplicadas ni ruta nueva.
- `go test -count=1 ./tests/unit/story/transport/http`: PASS exit 0; `go test ./...`: PASS exit 0, integración PostgreSQL cacheada; `git diff --check`: PASS. TDD Cycle Evidence tarea 8: RED inicial `6d2a2c0` → GREEN `1a92f72` → TRIANGULATE RED `75e5f8d` → GREEN focalizado y runner PASS → REFACTOR pendiente. Tarea 8 sigue `- [ ]`; 9–10 también `- [ ]` en `tasks.md`.
- Base PR 5 `7f9eb80`, cadena local `stacked-to-main`, límite 400; diff medido abajo, sin commits, integración ni despliegue. Reversión: retirar únicamente corrección de decoder y esta evidencia. Sin desviaciones de diseño. Estado nativo v2 consumido: apply ready, 7/10, next apply, sin bloqueos; actionContext repo-local con raíz autorizada del repo, sin advertencias. Estado posterior no consultado.

## Corte 5 — REFACTOR revisado, tarea 8 cerrada

- Cronología TDD: RED `6d2a2c0` (import ausente) → GREEN `1a92f72` → TRIANGULATE RED `75e5f8d` (criterio null recibió 422) → GREEN `91dde9f` (400 antes de validar) → REFACTOR: revisión sin simplificación útil; no se cambiaron fuente ni pruebas. La doble lectura JSON preserva explícitamente distinción de tipo para elementos null y mantiene `DisallowUnknownFields`; una reescritura estética no mejora claridad. Sin desviaciones de diseño.
- `go test -count=1 ./tests/unit/story/transport/http`: PASS exit 0; `go test ./...`: PASS exit 0 (integraciones cacheadas, no prueba fresca de PostgreSQL); `git diff --check`: PASS exit 0. Tras los checks, tarea 8 marcada `- [x]` en `tasks.md`; pendientes exactos `- [ ] 9.` y `- [ ] 10.` allí, sin iniciar PR 6. Solo tareas/progreso cambian en este cierre; handler y tests siguen en commits previos.
- PR 5 `stacked-to-main` base real `7f9eb80`, sin publicación/merge ni registro de ruta; diff medido contra base +362/−2 = **364 líneas** (margen 36) ≤400. `.pi/` sin seguimiento intacta. Reversión del slice: retirar handler, tests y registros/casillas de PR 5, sin afectar almacenamiento ni US-01. Estado nativo v2 consumido: apply ready, 7/10 antes de marcar 8, next apply, sin bloqueos; actionContext repo-local con workspaceRoot/allowedEditRoots raíz autorizada del repositorio, sin advertencias. No se consultó estado posterior.

## Corte 6 — tarea 9 RED; detenerse para commit gate

- Base `ec0850d` en `feat/us05-story-http`, cadena local `stacked-to-main` dependiente de PR 5; solo pruebas y evidencia, sin producción, commit, push, merge ni despliegue. Casilla 9 marcada `[x]` en `tasks.md` después del RED; casilla 10 sigue `[ ]` (línea exacta allí). `.pi/` intacta. Reversión: retirar los tests de este corte y esta evidencia/casilla.
- Tests: `tests/unit/cmd/api/main_test.go` comprueba ruta POST, otros métodos sin escritura y preservación de `POST /projects`; `tests/integration/story/postgres/http_integration_test.go` usa `storyDatabase` (migraciones 000001/000002), proyecto existente, respuesta 201, fila vinculada y puntos SQL NULL, y proyecto ausente 404 sin fila adicional. API de composición propuesta, compatible con llamadas antiguas mediante argumento variádico opcional: `api.NewHTTPHandler(projectRepo, projectIDGenerator, api.StoryDependencies{Repository: storyRepo, GenerateID: storyIDGenerator})`. Pendiente de diseño GREEN: decidir inyección segura en arranque para no publicar ruta sin migración.
- Safety net antes de editar: `go test -count=1 ./tests/unit/cmd/api ./tests/integration/story/postgres` PASS, PostgreSQL real (8.056s). `go test ./...` **FAIL exit 1**: ambos paquetes nuevos fallan compilación porque `api.StoryDependencies` no existe y `NewHTTPHandler` solo admite dos argumentos; aserciones HTTP nuevas **no ejecutadas**. `go test -count=1 -v ./tests/integration/...` **FAIL exit 1**: story no compila por la misma API; project integration PASS real (2.623s), no se acredita HTTP PostgreSQL ni se reportan skips de story como ejecución. `git diff --check` PASS.

### TDD Cycle Evidence — tarea 9

| Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- |
| Unit API y story PostgreSQL PASS antes del cambio | Runner e integración FAIL por composición aún ausente; ninguna aserción nueva corrió | Pendiente de parent gate (tarea 10) | Pendiente | Pendiente |

- Medida provisional corte 6: +111/−1 = **112 líneas** antes de esta evidencia (45 unitarias, 65 integración, casilla +1/−1); con esta sección ~122–130. Pronóstico del corte completo **~270–360** líneas si composición, arranque, README, borde TRIANGULATE y evidencia añaden ~145–230; dejar margen respecto a 400 y medir de nuevo antes de GREEN, sin minificar ni omitir pruebas. No hay desviación de contrato; RED por compilación, no por assertion runtime. Estado nativo v2 consumido: `applyState: ready`, 8/10 antes de marcar 9, `nextRecommended: apply`, `blockedReasons: []`, `notes: []`; `actionContext: repo-local`, workspaceRoot/allowedEditRoots raíz canónica autorizada, sin advertencias. Estado posterior no consultado.
