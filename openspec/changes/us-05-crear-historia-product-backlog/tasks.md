# Tareas: US-05 — crear historia en el Product Backlog (#33)

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 1240–1900 en total, incluyendo SDD pendiente de incorporar, código, pruebas, migración y README (adiciones + eliminaciones; estimación, no diff medido) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 planificación base → PR 2 tareas → PR 3 dominio/aplicación → PR 4 persistencia/FK → PR 5 handler aislado → PR 6 integración HTTP/documentación |
| Delivery strategy | ask-on-risk |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

**Decisión ya confirmada:** cortes encadenados hacia `main`, máximo **400 adiciones + eliminaciones por PR**, sin `size:exception`. Las casillas registran solo trabajo verificado: la tarea 1 ya se completó en el corte documental anterior; la tarea 2 se completa tras validar este corte, y las tareas 3–10 esperan implementación. Estos dos cortes no cambian código ni ejecutan pruebas runtime; no se publica PR, hace push, merge ni elimina ramas. `ask-on-risk` sigue vigente: si un corte no cabe, detener apply y consultar antes de cambiar alcance/estrategia; jamás dar por aprobada una excepción. Las cifras por corte son rangos orientativos con objetivo operativo **≤350** para dejar ≥50 líneas de margen; recalcular el diff real contra la base de cada PR antes de prepararlo. Contar fuente, pruebas, SQL, README y artefactos SDD existentes que efectivamente entren en el diff, incluidas eliminaciones por reescritura; no tratar un archivo sin seguimiento como «0 líneas». Si algún documento ya está incorporado en la base, contar solo su diff real, sin inventar un estado de Git. No hay medición de `git status`/diff en esta revisión: el riesgo residual del corte 4–6 y de los documentos sin incorporar es **medio/alto** hasta medirlos. Si un corte excede 350, reducir/split por comportamiento autocontenido; si excede 400, no abrirlo como una PR de ese tamaño: pausar y replanificar con el usuario.

**Orden de entrega seleccionado:** `main` ← PR 1 `docs/us05-plan-base` ← PR 2 `docs/us05-tasks` ← PR 3 `feat/us05-story-core` ← PR 4 `feat/us05-story-storage` ← PR 5 `feat/us05-story-handler` ← PR 6 `feat/us05-story-http`. En el estado local actual, PR 1 aún no está integrado en `main`: `docs/us05-tasks` parte de `docs/us05-plan-base` (`654dcb8`), no de `main`. El diff aislado de este corte se mide contra ese commit padre; no presentar el diff acumulado contra `main` como tamaño de PR 2. Cada PR apuntará a `main` **después** de integrar la anterior; antes de eso, la rama hija depende de la anterior y deberá retarget/rebase tras su merge para que el diff de revisión muestre solo su unidad. No registrar `POST /projects/{project_id}/stories` hasta que el núcleo, la migración `000002`, la FK y la persistencia verificadas estén integrados en `main`; antes de exponer la ruta en cualquier entorno, aplicar `migrate -path internal/project/infrastructure/postgres/migrations -database "$DATABASE_URL" up`. Si no es posible asegurar ese orden de despliegue, mantenerla sin publicar y consultar.

**Evidencia futura:** `openspec/config.yaml` activa TDD estricto para los cortes de comportamiento 3–6: commits cronológicos **RED → GREEN → TRIANGULATE → REFACTOR** en cada corte, `go test ./...` en cada paso con resultado registrado (RED fallido, incluso si falla compilación; GREEN/TRIANGULATE final/REFACTOR en verde); las pruebas de cada conducta quedan con su código en la misma PR. TRIANGULATE agrega un caso límite primero RED y luego lo satisface antes de REFACTOR; si un corte necesita varios ciclos, mantener el orden y documentar cada uno. Los PR 1–2 solo documentan un plan, por lo que TDD de ejecución no aplica allí; comprobar coherencia de rutas, reglas y tamaño del diff. En PR 4 y PR 6 ejecutar además integración real con Docker (`go test -count=1 -v ./tests/integration/...`); un skip no es prueba de integridad. Verificar cada rama contra su base y el estado final de sus pruebas; los commits RED intermedios pueden fallar y no son estados listos para integrar. El cierre de #33 corresponde únicamente al corte final tras verificar todo; no hacer cierre prematuro.

## PR 1 — decisiones SDD revisables (250–340 líneas estimadas)

Inicio: `main` con US-01 sin cambios; base `main`, rama propuesta `docs/us05-plan-base`. Fin: incorporar, **si aún no están en la base**, `openspec/changes/us-05-crear-historia-product-backlog/{exploration.md,proposal.md,specs/historia/spec.md,design.md}` como decisiones históricas de producto/contrato, sin código ni ruta pública. Verificación: contrastar reglas entre los cuatro archivos, confirmar que aún no se ofreció creación HTTP y contar adiciones + eliminaciones reales de estos documentos contra `main` (objetivo ≤350, tope 400). Reversión: revertir solo estos artefactos SDD, sin tocar US-01 ni datos. Sigue PR 2 (plan operativo); fuera de alcance: implementación y pruebas de runtime. **No reinterpretar los comentarios históricos «pendiente de decidir estrategia» en proposal/design como veto a la elección posterior del usuario:** esta revisión de `tasks.md` documenta esa elección.

- [x] 1. Revisar el diff de `openspec/changes/us-05-crear-historia-product-backlog/{exploration.md,proposal.md,specs/historia/spec.md,design.md}` contra `main`, preservar sus decisiones históricas y comprobar que PR 1 es documentación coherente y ≤400 líneas cambiadas; si no cabe, dividir la documentación en unidades autónomas antes de avanzar.

## PR 2 — plan operativo de entrega (140–220 líneas estimadas)

Inicio real: PR 1 todavía no integrado; base local `docs/us05-plan-base` en `654dcb8`, rama `docs/us05-tasks`. Base prevista para revisión: `main` actualizado **tras integrar PR 1**. Fin: incorporar **este** `openspec/changes/us-05-crear-historia-product-backlog/tasks.md` y la evidencia incremental de `apply-progress.md`, sin modificar los archivos históricos de PR 1 ni publicar funcionalidad. Verificación: revisar casillas, estrategia seleccionada, dependencias y consistencia con spec/design; medir adiciones **y eliminaciones** reales de ambos archivos contra `654dcb8` antes del commit y tras él. Al integrarse PR 1, comprobar el diff aislado contra `main` actualizado antes de preparar PR 2. Reversión: revertir solo tareas y evidencia de este corte, sin retirar el contrato SDD histórico. Sigue PR 3; fuera de alcance: código, migración y ejecución de pruebas de aplicación.

- [x] 2. Comprobar en `openspec/changes/us-05-crear-historia-product-backlog/tasks.md` las bases, salidas y topes; incorporar su diff real de planificación en PR 2 (≤400) sin marcar tareas como terminadas por redactar el plan.

## PR 3 — creación interna validada, sin acceso público (180–300 líneas estimadas)

Inicio real del correctivo: PR 2 en `9a4924bb` todavía no integrado en `main`; rama hija `feat/us05-story-core-tdd` desde ese commit. Base prevista para revisión: `main` actualizado tras integrar PR 2 y comprobar el diff aislado. US-01 intacta. Fin: caso de uso invocable internamente, no registrado en `internal/api/api.go`, con una única escritura vía puerto, sin implementar todavía PostgreSQL. Verificación: `go test ./...` con casos unitarios y ausencia de endpoint; ninguna dependencia runtime nueva requiere migración. Reversión: retirar `internal/story/{domain/story.go,application/create_story.go}` y `tests/unit/story/{domain/story_test.go,application/create_story_test.go}`; no datos a migrar. Sigue PR 4 (FK y repo); fuera de alcance: endpoint, consulta de backlog y estimación.

- [x] 3. **RED:** en `tests/unit/story/domain/story_test.go` y `tests/unit/story/application/create_story_test.go`, probar título/descripción en blanco (ASCII/Unicode), prioridad exacta `alta/media/baja`, criterios ausentes/nulos/vacíos o con entrada en blanco, conservación de texto/orden, estado `pendiente`, puntos ausentes, ID generado, una escritura válida, cero ante invalidación y propagación de error tipado de proyecto ausente o inesperado; ejecutar `go test ./...` y registrar el RED.
- [x] 4. **GREEN → TRIANGULATE → REFACTOR:** implementar `internal/story/domain/story.go` y `internal/story/application/create_story.go` con validación por campo, comando limitado a ProjectID y cuatro entradas, generador ID, puerto `Create(ctx, story) error` y `ErrProjectNotFound` desacoplado de pgx. Reejecutar `go test ./...` tras GREEN; agregar primero un borde RED (p. ej. criterios Unicode), resolverlo y simplificar sin ampliar contrato, dejando el runner verde tras cada etapa final. No introducir lectura previa ni aceptar ID/estado/puntos del cliente.

## PR 4 — almacenamiento e integridad referencial (230–350 líneas estimadas)

Inicio: PR 3 integrado; base `main` actualizado, rama `feat/us05-story-storage`. Fin: migración 000002 y repositorio invocable internamente, FK `ON DELETE RESTRICT` probada en PostgreSQL real, **sin registrar endpoint**. Antes de ejecutar el repositorio en entornos que lo invoquen, aplicar migración externamente; no cambiar el arranque de US-01. Verificación: `go test ./...` y `go test -count=1 -v ./tests/integration/...` con Docker real, sin skips de historias; comprobar `story_points IS NULL`, ninguna historia huérfana, FK nombrada y errores inesperados sin traducción falsa. Reversión: retirar repo/tests y evaluar datos antes de `000002_create_stories.down.sql` (borra solo `stories`); nunca borrar `projects` ni usar down con historias sin decisión explícita sobre datos. Sigue PR 5; fuera de alcance: ruta pública y HTTP.

- [x] 5. **RED:** crear `tests/integration/story/postgres/repository_integration_test.go` con Testcontainers y migraciones `000001` + `000002` (referencia: `tests/integration/project/postgres/repository_integration_test.go`): proyecto presente/faltante, fila y orden `TEXT[]`, `story_points IS NULL`, `23503` de FK nombrada frente a otro `23503`/error inesperado, rechazo de borrado de proyecto referenciado y órdenes controlados inserción/borrado sin huérfanos. Ejecutar `go test ./...` y registrar RED; Docker requerido, skip no acredita RED de persistencia.
- [x] 6. **GREEN → TRIANGULATE → REFACTOR:** añadir `internal/project/infrastructure/postgres/migrations/000002_create_stories.{up,down}.sql` y `internal/story/infrastructure/postgres/repository.go`: PK/FK nombrada `stories_project_id_fkey`, checks básicos, `story_points INTEGER NULL` (US-14 podrá actualizarlo), INSERT único parametrizado sin SELECT, traducir solo `23503` de esta FK a `ErrProjectNotFound`; no reintentar resultado de red incierto. Ejecutar `go test ./...` tras GREEN, añadir borde RED de integridad y resolverlo, simplificar, y al cerrar ejecutar runner y comando de integración con Docker **sin skips**. Si la carrera o Testcontainers elevan el diff hacia 400, separar un borde de integridad en otro PR interno completo antes del endpoint, no omitir pruebas.

**Decisión posterior limitada a PR 4 (slice4):** el usuario rechazó dividir este corte y aceptó explícitamente `size:exception` solo aquí (pronóstico 430–600 líneas). `delivery_strategy=exception-ok` aplica únicamente a este corte; `chain_strategy=stacked-to-main` continúa y todos los demás cortes conservan el tope de 400. La rama real `feat/us05-story-storage` parte de `feat/us05-story-core-tdd` en `9321106cb4c727dc6c2fd8a17bf0df66385348ad`, no de `main`: las PR previas siguen sin integrar. Esta decisión posterior sustituye solo el tope histórico de PR 4, no altera tareas ni decisiones anteriores.

## PR 5 — adaptador HTTP aislado (240–350 líneas estimadas)

**Decisión posterior:** el usuario autorizó desarrollo y pruebas locales encadenados de todo #33 antes de integrar en `main`; la condición histórica de integración previa de PR 4 aplica ahora a publicación/despliegue, no al trabajo local. PR 4 cerró en `7f9eb80`; base real de `feat/us05-story-handler` es ese commit en `feat/us05-story-storage`. Se conserva `stacked-to-main`, tope 400 para PR 5 y ninguna autorización de merge/despliegue. El inicio previsto a continuación se conserva como plan histórico de revisión.

Inicio: PR 4 integrado, migración/FK disponibles; base `main` actualizado, rama `feat/us05-story-handler`. Fin: `internal/story/transport/http/handler.go` probado directamente mediante handler y dobles del caso de uso/repositorio; `internal/api/api.go` sigue sin registrar la ruta. Verificación: `go test ./...`, respuesta/errores seguros y ninguna escritura por input inválido; no se necesitan datos reales para tests de handler. Reversión: retirar handler y `tests/unit/story/transport/http/handler_test.go`, sin tocar persistencia ni US-01. Sigue PR 6 (composición, prueba HTTP real, README); fuera de alcance: exponer endpoint.

- [x] 7. **RED:** crear `tests/unit/story/transport/http/handler_test.go` con `PathValue` en requests directas y casos `201` (ID/ProjectID UUID, `pendiente`, prioridad, criterios ordenados, clave `story_points:null`), `400 invalid_request` fijo para JSON mal formado, múltiples valores, tipos, cuerpo `null`/array y claves extra incluidas `id`, `project_id`, `status`, `story_points` aun con null/0; `422 validation_failed` para UUID y entradas inválidas sin escritura, `404` solo error tipado y `500` genérico sin secretos; ejecutar `go test ./...` y registrar RED.
- [x] 8. **GREEN → TRIANGULATE → REFACTOR:** implementar `internal/story/transport/http/handler.go` con objeto JSON único, `DisallowUnknownFields`, UUID normalizado antes de DB, mapeos seguros 400/422/404/500 y `story_points` nulo sin `omitempty`; ejecutar `go test ./...` tras GREEN, añadir primero un caso RED de rechazo (p. ej. Unicode/prioridad con mayúsculas), resolverlo y refactorizar con runner verde. No registrar la ruta en `internal/api/api.go` en este corte.

## PR 6 — habilitación de ruta con persistencia integrada (200–340 líneas estimadas)

Inicio: PR 5 integrado, FK/repositorio verificados e instalación de migración externa disponible; base `main` actualizado, rama `feat/us05-story-http`. Fin: `POST /projects/{project_id}/stories` operativo sobre DB migrada; `POST /projects` intacto y README actualizado. Verificación: `go test ./...`, `go test -count=1 -v ./tests/integration/...` con Docker real y ejercicio HTTP de alta válida/proyecto inexistente contra DB migrada; comprobar `201`, `404`, nulidad y fila vinculada, sin skips. Reversión: desregistrar la ruta y revertir composición, tests HTTP y sección README sin revertir PR 4; evaluar historias existentes antes de cualquier down de migración. Seguimiento: US-06/07/09/14 por separado; fuera de alcance: listado/UI, edición, Sprint, Planning Poker, reintentos/idempotencia. Si la instalación 000002 no precede la puesta en servicio, **no publicar** esta PR en un entorno con la ruta expuesta.

- [x] 9. **RED:** ampliar `tests/unit/cmd/api/main_test.go` para registro de nueva ruta/método no creador y preservación `POST /projects`; agregar prueba HTTP real a `tests/integration/story/postgres/repository_integration_test.go` (o archivo concreto `tests/integration/story/postgres/http_integration_test.go`) con migraciones aplicadas, alta y proyecto inexistente. Ejecutar `go test ./...` y registrar RED con Docker; no acreditar integración omitida.
- [ ] 10. **GREEN → TRIANGULATE → REFACTOR:** adaptar `internal/api/api.go` y `cmd/api/main.go` para inyectar repo PostgreSQL/generador UUID, registrar ruta solo tras disponibilidad de 000002; actualizar `README.md` con request/response, cuatro campos, ProjectID conocido, comando `migrate ... up` previo al servidor y advertencia sobre rollback con datos. Ejecutar `go test ./...` tras GREEN; añadir un caso RED de composición/HTTP, resolverlo y refactorizar; ejecutar runner e integración Docker reales al cierre, comprobar diff ≤400 incluyendo README/tests y verificar manualmente alta/rechazo contra DB migrada. No modificar US-01 ni cerrar #33 antes de la verificación final.
